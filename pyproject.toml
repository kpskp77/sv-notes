[project]
name = "sv"
version = "0.1.0"
description = "SystemVerilog Learning Notes"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
  "mkdocs-material",
  "mkdocs-enumerate-headings-plugin",
]